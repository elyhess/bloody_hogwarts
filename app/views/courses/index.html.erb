<h1>All Courses:</h1>

<% @courses.each do |course| %>
  <section id="course-<%= course.id %>">
    <p class='course-name'><%= course.name %> has <%= course.students.student_count %> students</p>
  </section>
<% end %>