<h1>All Courses:</h1>

<% @courses.each do |course| %>
  <section id="course-<%= course.id %>">
    <h2><%= course.name %> has <%= course.students.student_count %> students</h2>
  </section>
<% end %>